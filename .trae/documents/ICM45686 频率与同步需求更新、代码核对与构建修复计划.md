## 文档更新
- 更新 `boards/st/nucleo-h743zi-fc/需求.md`：
  - 芯片型号：SPI1/SPI3 读取 ICM45686 两颗（与 `boards/px4/fmu-v6x` 同款，驱动可复用）。
  - 频率：IMU 采样目标 1000 Hz；融合输出四元数 120 Hz；MAVLink 流中 `ATTITUDE_QUATERNION` 设为 120 Hz，`HIGHRES_IMU` 设为 120 Hz（避免串口拥塞）。
  - 同步与时间戳：保留 GPIO EXTI 帧/行同步用作时间戳，和相机 CMOS 配合实现防抖与滚动快门补偿（若后续硬件确需 I2C 方式同步，再新增说明与实现）。

## 搜索核对（使用搜索代理）
- 驱动一致性：
  - 在仓库中检索 v6x 板使用的 ICM45686 启动命令与驱动实现位置（确认是否通过 `icm42688p -6` 变体启用 ICM-42686）。
  - 检索 ICM 驱动的发布频率控制点（FIFO 水位、`ScheduleOnInterval`），确认实现 1000 Hz 发布的可行性或默认策略。
- MAVLink 流语法与频率：
  - 确认 `mavlink stream -s <NAME> -r <Hz>` 设置 120 Hz 的语法与支持范围。
- ScheduledWorkItem 调度：
  - 核对融合模块的调度周期设置点（改为 8333 µs ≈ 120 Hz）。

## 代码修正（在确认后实施）
- `rc.board_sensors`：
  - 若搜索确认 ICM-42686 通过 `icm42688p -6` 启用，则将启动行改为 `icm42688p start -s -b 1 -R 0 -6` 与 `-b 3 -R 8 -6`；否则保持 `icm45686 start` 形式并对齐 v6x。
  - 保持 `bmm150 start -I -b 1 -R 0`。
  - 将 MAVLink 流速改为 `-r 120`（ATTITUDE_QUATERNION 与 HIGHRES_IMU）。
- 融合模块：
  - 将 `ScheduleOnInterval(8333)` 以实现 120 Hz 发布；保持订阅两路 IMU 与磁力计，输出 `vehicle_attitude`。
- 板级与 Kconfig 保持现有最小集（MAVLink、Sensors、Dataman、cmos_sync、dual_imu_fusion），不做不必要更改。

## 构建与验证
- WSL：`make distclean && make st_nucleo-h743zi-fc_default -j4`。
- 核对：生成 `.px4` 包；`px4_boardconfig.h` 串口映射 `TELEM1=/dev/ttyS2`；ROMFS `rc.board_sensors` 含 ICM 启动与 120 Hz 流；融合模块调度为 8333 µs。
- 运行级（连接硬件后）：
  - `icm45686 status`（或 `icm42688p status` 视驱动形式）、`bmm150 status`、`sensors status`。
  - `listener vehicle_attitude`（≈120 Hz）与 `listener gpio_in`（帧/行事件）。
  - `mavlink status` 与流速（ATTITUDE_QUATERNION/HIGHRES_IMU 120 Hz）。

## 交付
- 更新后的需求文档。
- 搜索结果摘要（驱动与频率支持点）。
- 修正后的启动脚本与融合调度实现。
- 通过的 WSL 构建产物与验证清单。

## 备注
- 不使用注释禁用功能规避编译；所有关闭/修改均通过 Kconfig 与脚本显式配置；如未来确认 CMOS 同步需改为 I2C 路径，再追加实现与说明。